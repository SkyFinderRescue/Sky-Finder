<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <title>Sky Finder</title>
  <style>
    :root{
      --bg:#08111f;
      --card:#101b2d;
      --card2:#16243b;
      --line:rgba(255,255,255,.12);
      --text:#f5f7fb;
      --muted:#c4d0e6;
      --accent:#ff5a36;
    }
    *{box-sizing:border-box;-webkit-tap-highlight-color:transparent}
    html,body{margin:0;padding:0;background:linear-gradient(180deg,#08111f,#0c1730);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial,sans-serif}
    body{min-height:100vh}
    .wrap{max-width:760px;margin:0 auto;padding:20px 16px 28px}
    .card{
      margin-top:14px;
      background:rgba(16,27,45,.96);
      border:1px solid var(--line);
      border-radius:22px;
      padding:18px;
      box-shadow:0 12px 30px rgba(0,0,0,.28);
    }
    h1{
      margin:0;
      font-size:40px;
      line-height:1.05;
      letter-spacing:-.02em;
    }
    .sub{
      margin-top:10px;
      color:var(--muted);
      font-size:18px;
      line-height:1.4;
    }
    .btns{display:grid;grid-template-columns:1fr;gap:12px;margin-top:18px}
    a.btn, button.btn{
      appearance:none;
      border:0;
      border-radius:18px;
      min-height:62px;
      padding:16px 18px;
      font-size:21px;
      font-weight:800;
      text-decoration:none;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      cursor:pointer;
    }
    .primary{
      background:linear-gradient(180deg,#ff7a5c,#ff5a36);
      color:#fff;
      box-shadow:0 10px 24px rgba(255,90,54,.24);
    }
    .secondary{
      background:var(--card2);
      color:var(--text);
      border:1px solid var(--line);
    }
    .status{
      margin-top:14px;
      padding:12px 14px;
      border-radius:16px;
      background:rgba(255,255,255,.06);
      border:1px solid var(--line);
      color:var(--muted);
      font-size:15px;
      line-height:1.45;
    }
    .legend{
      margin-top:14px;
      padding:14px;
      border-radius:16px;
      background:rgba(255,255,255,.06);
      border:1px solid var(--line);
      font-size:15px;
      line-height:1.5;
    }
    .legend b{color:#fff}
    .tiny{
      margin-top:14px;
      color:var(--muted);
      font-size:13px;
      line-height:1.45;
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>Sky Finder</h1>
      <div class="sub">Crashed Paraglider Rescue Locator for California</div>

      <div class="btns">
        <a class="btn primary" href="https://xcfind.paraglide.us/map.html?id=16" target="_blank" rel="noopener noreferrer">
          Open California Live Map
        </a>
        <button class="btn secondary" id="gpsBtn" type="button">Use My GPS</button>
      </div>

      <div class="status" id="gpsStatus">Tap <b>Use My GPS</b> to get your current coordinates.</div>

      <div class="legend">
        <b>Track number legend:</b> the numbers on the XCFind map are the pilot’s track points in order.
        <b>1</b> is an older point. Higher numbers are newer points.
        The <b>highest number</b> is usually the pilot’s <b>most recent visible location</b>.
      </div>

      <div class="tiny">
        This version opens XCFind directly instead of embedding it, which is more reliable on iPhone and GitHub Pages.
      </div>
    </div>
  </div>

  <script>
    const statusEl = document.getElementById('gpsStatus');
    const gpsBtn = document.getElementById('gpsBtn');

    function setStatus(text){
      statusEl.innerHTML = text;
    }

    function startGps(){
      if(!navigator.geolocation){
        setStatus('Geolocation is not supported on this device.');
        return;
      }

      setStatus('Requesting GPS…');

      navigator.geolocation.getCurrentPosition(
        (pos) => {
          const lat = pos.coords.latitude.toFixed(6);
          const lng = pos.coords.longitude.toFixed(6);
          setStatus('Your location: <b>' + lat + ', ' + lng + '</b>');
        },
        (err) => {
          setStatus('GPS error: ' + err.message);
        },
        { enableHighAccuracy:true, timeout:15000, maximumAge:10000 }
      );
    }

    gpsBtn.addEventListener('click', startGps);
  </script>
</body>
</html>
